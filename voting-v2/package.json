{
  "name": "voting-v2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ voting-v2",
    "create-local": "graph create --node http://localhost:8020/ voting-v2",
    "remove-local": "graph remove --node http://localhost:8020/ voting-v2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 voting-v2",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.86.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
